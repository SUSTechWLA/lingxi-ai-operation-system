# 项目启动说明（Windows 开发模式）

本文件是本仓库在 Windows 开发环境中的统一启动入口说明。

> **关键规则：** 本地历史项目保存于 `local-backend/data`。启动本地代理时必须显式传入该目录；不要直接执行未带参数的 `go run ./cmd/local-agent`，否则可能使用临时目录并显示为空项目列表。

开发模式由以下四个组件组成：

| 组件 | 默认地址 | 作用 | 是否通常需要启动 |
| --- | --- | --- | --- |
| 前端 | `http://localhost:3000` | Web 开发界面 | 是 |
| 本地后端（local-agent） | `http://127.0.0.1:18080` | 历史项目、文件、缓存与本地执行 | 是 |
| 标书工具 | `http://127.0.0.1:9001` | 招标文件和标书文件操作 | 标书流程需要时启动 |
| 云端后端 | `http://localhost:8080` | 模型调用、编排和云端 API | 需要云端能力时启动 |

## 1. 前置条件

- Go、Node.js/npm 已安装并可在 PowerShell 中运行。
- Python 已安装并可在 PowerShell 中运行；标书工具依赖 Flask。
- 首次运行前，在 `frontend` 目录执行一次 `npm.cmd install`。
- 仅在需要云端编排、远程模型调用或完整云端 API 时才需要 Docker Desktop。

## 2. 推荐启动方式：浏览器开发模式

使用三个 PowerShell 窗口。先启动本地代理和标书工具，再启动前端。

### 窗口 A：本地代理（必须先启动）

```powershell
$repo = 'E:\lingxi\tangying-ai-operation-system'
$env:TANGYING_LOCAL_DATA_DIR = "$repo\local-backend\data"
$env:TANGYING_WORKSPACE_ROOT = $repo
$env:BIAOSHU_OUTPUT_DIR = "$repo\biaoshu-tools\output"
$env:GOCACHE = "$repo\.gocache-local"

Set-Location "$repo\local-backend"
go run ./cmd/local-agent `
  -addr 127.0.0.1:18080 `
  -data-dir $env:TANGYING_LOCAL_DATA_DIR `
  -workspace-root $env:TANGYING_WORKSPACE_ROOT `
  -biaoshu-output-dir $env:BIAOSHU_OUTPUT_DIR
```

该窗口必须保持运行。它负责读取和写入项目历史、标书产物、本地文件、缓存与诊断信息。

### 窗口 B：标书工具

```powershell
$repo = 'E:\lingxi\tangying-ai-operation-system'
$env:BIAOSHU_OUTPUT_DIR = "$repo\biaoshu-tools\output"

Set-Location "$repo\biaoshu-tools"
python app.py --host 127.0.0.1 --port 9001
```

该窗口必须保持运行。它提供标书文件解析和本地文件操作服务。若提示缺少 Flask，执行：

```powershell
python -m pip install flask
```

### 窗口 C：前端

首次运行：

```powershell
Set-Location 'E:\lingxi\tangying-ai-operation-system\frontend'
npm.cmd install
```

日常启动：

```powershell
Set-Location 'E:\lingxi\tangying-ai-operation-system\frontend'
npm.cmd run dev
```

打开终端输出的地址，通常为 <http://localhost:3000>。

## 3. 启动后必须检查

本地代理健康接口：

```powershell
Invoke-RestMethod 'http://127.0.0.1:18080/api/local/health'
```

输出中的 `dataDir` 必须为：

```text
E:\lingxi\tangying-ai-operation-system\local-backend\data
```

检查历史项目是否可读取：

```powershell
Invoke-RestMethod 'http://127.0.0.1:18080/api/local/biaoshu/projects'
```

返回的 `projects` 数组应包含既有项目。

标书工具健康接口：

```powershell
Invoke-RestMethod 'http://127.0.0.1:9001/health'
```

## 4. 端口 18080 已被占用时

先检查当前代理实际使用的数据目录：

```powershell
Invoke-RestMethod 'http://127.0.0.1:18080/api/local/health'
```

如果 `dataDir` 不是本仓库的 `local-backend/data`，先停止占用 18080 的旧代理，再按第 2 节重新启动：

```powershell
$listener = Get-NetTCPConnection -LocalPort 18080 -State Listen
Stop-Process -Id $listener.OwningProcess
```

仅在确认该端口对应错误本地代理时执行该命令。

## 5. Electron 开发模式（可选）

Electron 开发模式需要 `frontend/resources/bin/tangying-local-agent.exe` 已存在。它会尝试自动启动本地代理和标书工具。若本地代理二进制不存在，仍可按第 2 节手动启动本地代理和标书工具，然后启动 Electron 前端：

```powershell
Set-Location 'E:\lingxi\tangying-ai-operation-system\frontend'
npm.cmd run electron:dev
```

不要同时让 Electron 自动启动的代理和手动代理争抢 `18080` 端口，也不要重复启动占用 `9001` 的标书工具。手动服务已运行时，Electron 应连接已有的正确服务。

## 6. 云端后端（仅在需要云端能力时启动）

本地标书历史与文件管理不依赖云端后端。需要云端 API、模型网关或编排时，在单独的 PowerShell 窗口运行：

```powershell
Set-Location 'E:\lingxi\tangying-ai-operation-system\cloud-backend'
Copy-Item .env.example .env
docker compose up -d
go build -o build\tangying-ai-os.exe cmd\tangying-ai-os\main.go
.\build\tangying-ai-os.exe
```

如果 `.env` 已存在，不要重复覆盖它。

## 7. 常用验证命令

```powershell
Set-Location 'E:\lingxi\tangying-ai-operation-system\local-backend'
$env:GOCACHE = 'E:\lingxi\tangying-ai-operation-system\.gocache-local'
go test ./...
```

```powershell
Set-Location 'E:\lingxi\tangying-ai-operation-system\frontend'
npm.cmd run test:biaoshu-chapters
npm.cmd run build
```

```powershell
Set-Location 'E:\lingxi\tangying-ai-operation-system\cloud-backend'
go test ./...
```

## 8. 数据目录说明

| 内容 | 开发模式位置 | 说明 |
| --- | --- | --- |
| 项目历史与项目清单 | `local-backend/data/projects/biaoshu/` | 页面“历史项目”的来源。 |
| 本地配置、对话与诊断 | `local-backend/data/config/`、`local-backend/data/diagnostics/` | 不要随意删除。 |
| 标书输出与章节初稿 | `biaoshu-tools/output/` | 包含任务书、章节 Markdown、项目镜像清单。 |
| Go 构建缓存 | `.gocache-local/` | 仅用于开发构建，可由 Go 重新生成。 |

## 9. 禁止的启动方式

以下命令会让本地代理自行选择默认数据目录，可能导致页面显示为空历史：

```powershell
go run ./cmd/local-agent
```

必须改用第 2 节的完整命令，或显式设置 `TANGYING_LOCAL_DATA_DIR` 与 `-data-dir`。

## 10. WSL/Linux 脚本

在已安装并配置好 Linux 发行版的 WSL/Linux 环境中，可以使用仓库脚本：

```bash
bash scripts/start-local-backend.sh
bash scripts/start-frontend.sh
```

当前 Windows 环境如果没有可用 WSL 发行版，请使用本文件第 2 节的 PowerShell 命令。
