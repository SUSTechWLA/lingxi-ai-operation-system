# 灵犀AI原生操作系统 (AIOS)

> **核心理念**: 像现代操作系统管理计算资源一样，AIOS 管理 AI 任务和工具，让开发者像写程序一样简单地调用 AI 能力。

---

## 📚 阅读指南

本文档面向**零基础读者**，帮助你快速理解整个系统。

### 什么是 AIOS？

想象一下：
- **Windows/Linux** 管理电脑的硬件资源（CPU、内存、硬盘）
- **AIOS** 管理 AI 能力（LLM、工具、外部 API）

就像写程序不需要懂操作系统内核一样，使用 AIOS 也不需要懂 AI 内部原理。

---

## 🏗️ 系统架构图

### 当前已实现模块

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                              用户入口层                                       │
│  ┌─────────────────────────────────────────────────────────────────────┐  │
│  │                    🌐 Web/CLI/SDK                                    │  │
│  └─────────────────────────────────────────────────────────────────────┘  │
└────────────────────────────────────┬────────────────────────────────────────┘
                                     │ 自然语言或结构化请求
                                     ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│                       自然语言翻译层 (NL-Translator)                         │
│  ┌─────────────────────────────────────────────────────────────────────┐  │
│  │ 用户说："写一篇关于AI的文章并生成摘要"                                   │  │
│  │                        ↓                                             │  │
│  │           转换成可执行的任务图（DAG）                                   │  │
│  └─────────────────────────────────────────────────────────────────────┘  │
│                              端口: 8081                                     │
└────────────────────────────────────┬────────────────────────────────────────┘
                                     │ REST API
                                     ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│                       任务编排层 (AI-Orchestrator)                            │
│  ┌─────────────────────────────────────────────────────────────────────┐  │
│  │  DAG 可视化：                                                         │  │
│  │                                                                      │  │
│  │    ┌──────────┐         ┌──────────┐         ┌──────────┐        │  │
│  │    │ 写文章   │ ──────▶ │ 生成摘要  │ ──────▶ │ 格式化   │        │  │
│  │    │ (LLM)   │ success │ (LLM)    │ success │ (TOOL)   │        │  │
│  │    └──────────┘         └──────────┘         └──────────┘        │  │
│  │        │                   │                    │                 │  │
│  │     RUNNING             READY                PENDING               │  │
│  └─────────────────────────────────────────────────────────────────────┘  │
│                              端口: 8080                                     │
│  ┌─────────────────────────────────────────────────────────────────────┐  │
│  │  Scheduler ────────▶ 事件驱动 ────────▶ Worker 执行                  │  │
│  │  节点调度器              │                   模拟执行                  │  │
│  │  StateMachine ─────▶ 状态管理                                          │  │
│  └─────────────────────────────────────────────────────────────────────┘  │
└────────────────────────────────────┬────────────────────────────────────────┘
                                     │ REST API
                                     ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│                       上下文管理层 (AI-Context)                              │
│  ┌─────────────────────────────────────────────────────────────────────┐  │
│  │  记录所有操作历史，支持回溯和故障恢复                                    │  │
│  │                                                                      │  │
│  │  TaskCreated ──▶ DAGValidated ──▶ NodeScheduled ──▶ NodeSuccess    │  │
│  │                                                                      │  │
│  │  支持节点快照：任意时刻的状态都可以恢复                                  │  │
│  └─────────────────────────────────────────────────────────────────────┘  │
│                              端口: 8082                                     │
└─────────────────────────────────────────────────────────────────────────────┘
```

### 🔧 工具执行层 (AI-Worker) - 规划中

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                          工具执行层 (AI-Worker)                              │
│                              端口: 8083 (规划)                              │
│  ┌─────────────────────────────────────────────────────────────────────┐  │
│  │  ┌─────────┐  ┌─────────┐  ┌─────────┐  ┌─────────┐            │  │
│  │  │  LLM    │  │  搜索   │  │  数据库  │  │  文件   │   ...       │  │
│  │  │  工具   │  │  工具   │  │   工具  │  │   工具  │            │  │
│  │  └─────────┘  └─────────┘  └─────────┘  └─────────┘            │  │
│  │       ↑            ↑            ↑            ↑                     │  │
│  │       │            │            │            │                       │  │
│  │       └────────────┴────────────┴────────────┘                       │  │
│  │                          │                                           │  │
│  │                    统一工具网关                                         │  │
│  └─────────────────────────────────────────────────────────────────────┘  │
│                                                                             │
│  工具注册表：                                                               │
│  ┌─────────────────────────────────────────────────────────────────────┐  │
│  │  工具名称        │ 类型     │ 描述              │ API 端点          │  │
│  │  ───────────────│──────────│──────────────────│──────────────────│  │
│  │  openai_llm     │ LLM      │ OpenAI GPT模型    │ /api/tools/llm   │  │
│  │  google_search   │ Search   │ 谷歌搜索          │ /api/tools/search │  │
│  │  postgres_query │ Database │ PostgreSQL查询    │ /api/tools/db     │  │
│  │  file_read      │ File     │ 读取文件          │ /api/tools/file   │  │
│  └─────────────────────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────────────────────┘
```

### 🔗 模块间通信详解

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                           完整请求生命周期                                     │
└─────────────────────────────────────────────────────────────────────────────┘

  1️⃣ 用户请求
  │
  │  POST /api/translate {"prompt": "写文章并生成摘要"}
  │
  ▼
┌───────────────┐
│ NL-Translator │  ① 接收自然语言
│   (8081)     │  ② 调用 OpenAI 转换为 DAG
└───────┬───────┘
        │  ③ REST: POST /api/task/create
        │  ④ REST: POST /api/task/{id}/dag
        ▼
┌───────────────┐
│  Orchestrator │  ⑤ 保存任务到 PostgreSQL
│   (8080)     │  ⑥ 发布事件到 Redpanda
└───────┬───────┘  ⑦ Scheduler 调度节点
        │  ⑧ 记录上下文
        │  ⑨ REST: POST /api/context/*
        ▼
┌───────────────┐
│  AI-Context   │
│   (8082)      │
└───────┬───────┘
        │  返回上下文历史
        │  支持快照恢复
        ▼

        【并行执行】← Redpanda 事件驱动

  ┌───────────────┐
  │  Scheduler    │  ⑩ 发布 ai.node.ready 事件
  │   调度器      │
  └───────┬───────┘
          │  ⑪ 从队列消费事件
          ▼
  ┌───────────────┐
  │ Worker Service │  ⑫ 执行节点（模拟/真实）
  │   (内置)     │  ⑬ 发布 ai.node.result 事件
  └───────┬───────┘
          │  ⑭ StateMachine 更新状态
          ▼
  ┌───────────────┐
  │  Orchestrator │  ⑮ 检查 DAG 完成状态
  │               │  ⑯ 更新任务状态
  └───────────────┘
```

### 📊 数据流向图

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                              数据流                                          │
└─────────────────────────────────────────────────────────────────────────────┘

请求数据：
  用户输入 ──▶ NL-Translator ──▶ GPT-4 ──▶ DAG结构
                                              │
                                              ▼
                                           Task创建
                                              │
                    ┌─────────────────────────┼─────────────────────────┐
                    │                         │                         │
                    ▼                         ▼                         ▼
            ┌───────────────┐         ┌───────────────┐         ┌───────────────┐
            │ PostgreSQL    │         │ Redpanda      │         │ AI-Context    │
            │ (持久化)       │         │ (事件流)       │         │ (历史记录)     │
            │               │         │               │         │               │
            │ • ai_task     │         │ • ai.node.*   │         │ • 上下文链    │
            │ • ai_node     │         │ • ai.task.*   │         │ • 快照数据    │
            │ • ai_context  │         │               │         │               │
            └───────────────┘         └───────────────┘         └───────────────┘
                    ▲                         │                         ▲
                    │                         │                         │
                    └─────────────────────────┴─────────────────────────┘
                                         查询/订阅
```

---

## 🎯 模块职责

### NL-Translator (端口 8081)
**做什么**：把"人话"翻译成"机器能懂的任务图"

```java
// 就像翻译软件
输入: "写文章并生成摘要"
输出: DAG结构 {
  nodes: [
    {id: "1", type: "LLM", name: "write_article"},
    {id: "2", type: "LLM", name: "summarize"}
  ],
  edges: [
    {from: "1", to: "2"}  // 2依赖1的结果
  ]
}
```

### AI-Orchestrator (端口 8080)
**做什么**：任务的"操作系统内核"，负责任务调度和状态管理

- 接收 DAG
- 按依赖顺序调度执行
- 管理节点状态（CREATED → RUNNING → SUCCESS/FAILED）
- 处理并发和重试

### AI-Context (端口 8082)
**做什么**：任务的"黑匣子"，记录一切，支持回溯

- 记录每个操作
- 保存节点快照（用于故障恢复）
- 查询历史状态

### AI-Worker (规划中, 端口 8083)
**做什么**：真正的"工具人"，执行具体任务

- 连接外部 API（搜索、数据库、文件等）
- 执行 LLM 调用
- 返回执行结果

---

## 🚀 快速开始

### 一键启动所有服务

```bash
cd /Users/wanglian/Projects/lingxi-ai-operation-system
./scripts/startup.sh
```

### 手动启动（分步骤）

```bash
# 1. 启动基础设施
docker compose up -d

# 2. 启动上下文服务
cd ai-context && mvn spring-boot:run &

# 3. 启动编排服务
cd ai-orchestrator && mvn spring-boot:run &

# 4. 启动翻译服务
cd ai-nl-translator && mvn spring-boot:run &
```

### 测试你的第一个任务

```bash
# 通过 NL-Translator 创建任务
curl -X POST http://localhost:8081/api/translate \
  -H "Content-Type: application/json" \
  -d '{"prompt": "写一篇关于AI的文章"}'

# 查询任务状态
curl http://localhost:8080/api/task/{返回的taskId}

# 查看执行上下文
curl http://localhost:8080/api/task/{taskId}/context
```

---

## 📂 项目结构

```
lingxi-ai-operation-system/
│
├── 🌐 用户接口层
├── nl-translator/                    # 自然语言 → DAG 翻译
│   └── 职责：解析用户意图，生成可执行的任务图
│
├── ⚙️ 核心编排层
├── ai-orchestrator/                 # 任务编排引擎
│   └── 职责：调度任务、管理状态、事件驱动
│
├── 📝 上下文层
├── ai-context/                     # 上下文管理
│   └── 职责：记录历史、支持回溯、快照恢复
│
├── 🔧 工具执行层 (规划中)
├── ai-worker/                      # 工具网关
│   └── 职责：执行具体工具、连接外部API
│
└── 🗄️ 基础设施
    ├── PostgreSQL 16                # 持久化存储
    ├── Redis 7.x                    # 缓存
    ├── Redpanda                     # 事件总线 (Kafka兼容)
    └── MinIO/Qdrant                 # 对象存储/向量检索
```

---

## 🔮 未来架构展望

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                           完整 AIOS 架构 (远景)                               │
└─────────────────────────────────────────────────────────────────────────────┘

                            ┌─────────────┐
                            │   用户      │
                            └──────┬──────┘
                                   │ HTTP/WS/gRPC
                                   ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│                              API Gateway                                      │
│                    认证、限流、路由、监控、聚合                                 │
└────────────────────────────────────┬────────────────────────────────────────┘
                                     │
        ┌────────────────────────────┼────────────────────────────┐
        │                            │                            │
        ▼                            ▼                            ▼
┌───────────────┐          ┌───────────────┐          ┌───────────────┐
│ NL-Translator │          │  Workflow     │          │  Agent        │
│   (8081)     │          │   Engine      │          │   Service     │
│   自然语言    │          │   (808x)     │          │   (808x)     │
└───────────────┘          └───────────────┘          └───────────────┘
        │                            │                            │
        └────────────────────────────┼────────────────────────────┘
                                     │
                                     ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│                          Orchestrator (8080)                                │
│   任务调度 ◄────────────── 事件总线 (Redpanda) ──────────────► Worker执行 │
│   状态管理                    消息队列                      工具调用         │
└────────────────────────────────────┬────────────────────────────────────────┘
                                     │
        ┌────────────────────────────┼────────────────────────────┐
        │                            │                            │
        ▼                            ▼                            ▼
┌───────────────┐          ┌───────────────┐          ┌───────────────┐
│  AI-Context   │          │ PostgreSQL    │          │  AI-Worker   │
│   (8082)     │          │               │          │   (8083)     │
│  上下文追踪   │          │  业务数据     │          │  工具执行    │
└───────────────┘          └───────────────┘          └───────────────┘
        │                            │                            │
        └────────────────────────────┼────────────────────────────┘
                                     │
                    ┌────────────────┴────────────────┐
                    │         外部服务                  │
                    │  OpenAI │ Google │ 数据库 │ 文件 │
                    └─────────────────────────────────┘
```

---

## 🎓 学习路径

### 第一阶段：跑起来
1. 运行 `./scripts/startup.sh`
2. 调用 API 创建任务
3. 观察任务执行过程

### 第二阶段：理解原理
1. 阅读 [ORCHESTRATOR_GUIDE.md](docs/ORCHESTRATOR_GUIDE.md) - 理解任务编排
2. 阅读 [NL_TRANSLATOR_GUIDE.md](docs/NL_TRANSLATOR_GUIDE.md) - 理解自然语言处理
3. 阅读 [AI-CONTEXT_GUIDE.md](docs/AI-CONTEXT_GUIDE.md) - 理解上下文管理

### 第三阶段：二次开发
1. 添加新的节点类型
2. 实现自定义 Worker
3. 扩展 NL-Translator

---

## 📖 文档索引

| 文档 | 内容 | 难度 |
|------|------|------|
| [ORCHESTRATOR_GUIDE.md](docs/ORCHESTRATOR_GUIDE.md) | 任务编排详解 | ⭐⭐ |
| [NL_TRANSLATOR_GUIDE.md](docs/NL_TRANSLATOR_GUIDE.md) | 自然语言翻译详解 | ⭐⭐ |
| [AI-CONTEXT_GUIDE.md](docs/AI-CONTEXT_GUIDE.md) | 上下文管理详解 | ⭐ |
| [WORKER_GUIDE.md](docs/WORKER_GUIDE.md) | 工具执行层详解 | ⭐⭐⭐ |

---

## ❓ 常见问题

**Q: 为什么需要 NL-Translator？**
A: 让用户可以用自然语言描述任务，降低使用门槛。

**Q: 为什么需要 Orchestrator？**
A: 管理复杂任务中的节点依赖、状态流转、错误处理。

**Q: 为什么需要 AI-Context？**
A: 提供可追溯性，支持故障恢复和调试。

**Q: AI-Worker 是什么？**
A: 未来规划中的模块，负责真正执行工具调用（搜索、数据库等）。

---

## License

MIT License
