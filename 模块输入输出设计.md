# 系统模块输入输出设计

## 一、整体架构修正说明

根据需求，系统架构已将Kafka替换为Redpanda，将Milvus替换为Qdrant，其他组件保持不变。

## 二、模块输入输出详细设计

### 1. 全链路安全管控体系 (SecurityService)

**核心功能**：
- 统一身份认证：用户Token校验、服务间调用鉴权
- 分级权限管控：普通操作/敏感操作/高危操作的权限校验
- 敏感操作二次确认：支付、下单、内容发布等操作的拦截与确认
- 全链路数据加密：用户凭证、敏感数据的加解密
- 恶意指令拦截：违规、违法、恶意请求的实时拦截
- 接口访问限流：针对用户、服务的流量限流与熔断

**输入输出格式**：

| 接口名 | 输入参数 | 输出参数 | 使用场景 |
|-------|---------|---------|--------|
| `AuthCheck` | `user_id`：用户ID<br>`token`：用户令牌<br>`service_name`：服务名称<br>`api_name`：接口名称 | `pass`：是否通过校验<br>`error_msg`：错误信息<br>`user_permission`：用户权限 | 所有跨模块请求的身份校验 |
| `SensitiveCheck` | `user_id`：用户ID<br>`operation_type`：操作类型<br>`operation_params`：操作参数 | `need_confirm`：是否需要二次确认<br>`pass`：是否通过校验<br>`error_msg`：错误信息 | 敏感操作的权限校验 |
| `Crypto` | `plaintext`/`ciphertext`：明文/密文<br>`crypto_type`：加密类型 | `result`：加密/解密结果<br>`success`：操作是否成功<br>`error_msg`：错误信息 | 数据加密与解密 |

### 2. 统一可观测&配置中心 (ConfigService)

**核心功能**：
- 全链路追踪：所有请求带唯一Trace ID，贯穿全流程
- 指标监控：每个模块的核心指标统一采集、可视化告警
- 统一配置管理：所有环境配置统一管理，分环境下发
- 全链路日志采集：所有模块的日志统一格式、统一存储

**输入输出格式**：

| 接口名 | 输入参数 | 输出参数 | 使用场景 |
|-------|---------|---------|--------|
| `ConfigGet` | `key`：配置键<br>`env`：环境<br>`service_name`：服务名称 | `value`：配置值<br>`success`：操作是否成功<br>`error_msg`：错误信息 | 获取系统配置 |

### 3. 接入层 - Java REST API网关 (GatewayService)

**核心功能**：
- 协议转换：对外REST API，对内自动转发为gRPC请求
- 统一路由：将用户请求转发到对应的核心服务
- 统一鉴权：所有请求先经过安全模块的身份校验
- 限流熔断：针对异常流量限流，针对故障服务熔断
- 跨域处理：对外前端调用的CORS跨域支持

**输入输出格式**：

| 接口名 | 输入参数 | 输出参数 | 使用场景 |
|-------|---------|---------|--------|
| `Chat` | `user_id`：用户ID<br>`conversation_id`：对话ID<br>`user_input`：用户输入<br>`stream`：是否流式输出 | `conversation_id`：对话ID<br>`content`：回复内容<br>`is_finish`：是否完成 | 与LLM的对话交互 |
| `ToolCall` | `user_id`：用户ID<br>`tool_name`：工具名称<br>`arguments`：工具参数 | `success`：操作是否成功<br>`result`：工具执行结果<br>`error_msg`：错误信息<br>`latency_ms`：执行延迟 | 调用工具执行操作 |
| `UserAuth` | `username`：用户名<br>`password`：密码 | `user_id`：用户ID<br>`token`：用户令牌<br>`permission`：用户权限 | 用户登录认证 |

### 4. Agent智能层 - LLM交互服务 (LLMService)

**核心功能**：
- 多模型统一适配：封装豆包、GPT、Claude等大模型API
- Prompt模板管理：统一管理系统Prompt、角色设定、工具调用Prompt
- Token计算与限流：自动计算Token消耗，控制请求频率
- 流式响应支持：支持大模型流式输出
- 调用日志与重试：自动记录LLM调用日志，失败自动重试

**输入输出格式**：

| 接口名 | 输入参数 | 输出参数 | 使用场景 |
|-------|---------|---------|--------|
| `ChatCompletion` | `model`：模型名称<br>`messages`：消息列表<br>`temperature`：温度参数<br>`stream`：是否流式输出<br>`functions`：可用函数列表 | 流式输出：<br>`content`：回复内容<br>`tool_call`：工具调用<br>`usage`：Token使用情况<br>`finish_reason`：结束原因 | 与大模型的对话交互 |
| `Embedding` | `text_list`：文本列表<br>`model`：模型名称 | `embeddings`：嵌入向量列表<br>`usage`：Token使用情况 | 文本向量化 |

### 5. Agent智能层 - 任务规划与编排服务 (TaskPlanningService)

**核心功能**：
- 用户意图识别：精准理解用户的核心需求、约束条件、执行要求
- 任务拆解与DAG编排：将复杂需求拆解为多步可执行的子任务
- 任务执行调度：按编排好的DAG，依次调度工具调用服务执行子任务
- 异常处理与重试：子任务执行失败时，自动判断是否重试、是否调整执行方案
- 长上下文管理：结合对话上下文管理服务，维护任务执行的全链路上下文

**输入输出格式**：

| 接口名 | 输入参数 | 输出参数 | 使用场景 |
|-------|---------|---------|--------|
| `PlanAndExecuteTask` | `user_id`：用户ID<br>`conversation_id`：对话ID<br>`user_input`：用户输入<br>`max_step`：最大步骤数 | `task_id`：任务ID<br>`status`：任务状态<br>`final_result`：最终结果<br>`error_msg`：错误信息 | 任务规划与执行 |
| `GetTaskStatus` | `task_id`：任务ID | `task_id`：任务ID<br>`current_step`：当前步骤<br>`progress`：进度<br>`status`：任务状态 | 获取任务执行状态 |

### 6. Agent智能层 - 工具调度与参数校验服务 (ToolService)

**核心功能**：
- 工具注册与发现：从第三方平台对接服务拉取所有可用工具的定义
- 工具匹配与参数生成：基于用户需求，匹配最优工具，生成符合工具要求的入参
- 工具调用执行：通过gRPC调用第三方平台对接服务的工具接口
- 工具结果解析：将工具返回的结构化结果，解析为LLM可理解的文本格式
- 工具调用审计：将所有工具调用事件发送到Redpanda，供审计日志服务消费

**输入输出格式**：

| 接口名 | 输入参数 | 输出参数 | 使用场景 |
|-------|---------|---------|--------|
| `CallTool` | `user_id`：用户ID<br>`tool_name`：工具名称<br>`arguments`：工具参数 | `success`：操作是否成功<br>`result`：工具执行结果<br>`error_msg`：错误信息<br>`latency_ms`：执行延迟 | 调用工具执行操作 |
| `ListTools` | `platform`：平台名称<br>`keyword`：关键词 | `tools`：工具定义列表 | 列出可用工具 |

### 7. Agent智能层 - 对话上下文管理服务 (ConversationService)

**核心功能**：
- 对话历史管理：存储、查询用户的多轮对话历史，自动截断超长上下文
- 会话状态管理：维护用户当前的任务执行状态、会话上下文
- 用户偏好学习：提取用户的使用习惯、偏好设置，优化后续的任务规划
- 上下文向量化：将对话历史向量化存储，支持语义检索

**输入输出格式**：

| 接口名 | 输入参数 | 输出参数 | 使用场景 |
|-------|---------|---------|--------|
| `GetConversationHistory` | `user_id`：用户ID<br>`conversation_id`：对话ID<br>`max_round`：最大轮数 | `messages`：消息列表 | 获取对话历史 |
| `AppendConversationMessage` | `user_id`：用户ID<br>`conversation_id`：对话ID<br>`role`：角色<br>`content`：内容 | `success`：操作是否成功<br>`error_msg`：错误信息 | 追加对话消息 |
| `GetUserPreference` | `user_id`：用户ID | `preference`：用户偏好 | 获取用户偏好 |

### 8. 业务服务层 - 用户中心与权限服务 (UserService)

**核心功能**：
- 用户管理：用户注册、登录、信息修改、账号注销
- 权限管理：用户角色、权限分级、资源访问控制
- 凭证管理：用户各平台的授权凭证加密存储、生命周期管理
- OAuth2授权：对接第三方平台的OAuth2授权流程，获取用户授权凭证

**输入输出格式**：

| 接口名 | 输入参数 | 输出参数 | 使用场景 |
|-------|---------|---------|--------|
| `GetUserInfo` | `user_id`：用户ID | `user_id`：用户ID<br>`username`：用户名<br>`permission`：用户权限<br>`status`：用户状态 | 获取用户信息 |
| `GetUserCredential` | `user_id`：用户ID<br>`platform`：平台名称 | `encrypted_credential`：加密凭证<br>`expire_time`：过期时间 | 获取用户平台凭证 |
| `SaveUserCredential` | `user_id`：用户ID<br>`platform`：平台名称<br>`credential`：凭证 | `success`：操作是否成功<br>`error_msg`：错误信息 | 保存用户平台凭证 |

### 9. 业务服务层 - 第三方平台对接服务 (PlatformService)

**核心功能**：
- 平台API适配：对接各平台官方开放API，处理鉴权、签名、重试、限流、错误处理
- 工具标准化封装：将平台API封装为符合LLM Function Calling规范的标准化工具
- 工具注册：将所有工具定义注册到工具注册中心，供工具调度服务发现
- 接口监控：监控平台API的调用成功率、延迟、错误率，异常告警

**输入输出格式**：

| 接口名 | 输入参数 | 输出参数 | 使用场景 |
|-------|---------|---------|--------|
| `CallTool` | `user_id`：用户ID<br>`tool_name`：工具名称<br>`arguments`：工具参数 | `success`：操作是否成功<br>`result`：工具执行结果<br>`error_msg`：错误信息<br>`latency_ms`：执行延迟 | 调用平台工具执行操作 |
| `GetToolDefinitions` | `platform`：平台名称 | `tools`：工具定义列表 | 获取平台工具定义 |

### 10. 业务服务层 - 审计日志与合规服务 (AuditService)

**核心功能**：
- 操作审计：异步消费Redpanda中的工具调用、用户操作、敏感操作事件
- 合规审核：对用户输入、工具调用结果、发布内容进行合规审核
- 日志查询：提供审计日志的查询接口，支持按用户、操作类型、时间范围追溯
- 合规报表：定期生成合规报表，满足监管要求

**输入输出格式**：

| 接口名 | 输入参数 | 输出参数 | 使用场景 |
|-------|---------|---------|--------|
| `QueryAuditLog` | `user_id`：用户ID<br>`operation_type`：操作类型<br>`start_time`：开始时间<br>`end_time`：结束时间 | `logs`：审计日志列表 | 查询审计日志 |

### 11. 基础设施服务层 - 向量检索引擎服务 (VectorService)

**核心功能**：
- 向量写入：支持批量写入向量数据，自动构建索引
- 高性能向量检索：毫秒级的TopK相似向量检索，支持条件过滤
- 向量管理：支持向量的更新、删除、版本管理
- 索引优化：针对业务场景优化索引参数，提升检索准确率和速度

**输入输出格式**：

| 接口名 | 输入参数 | 输出参数 | 使用场景 |
|-------|---------|---------|--------|
| `SearchVector` | `collection_name`：集合名称<br>`vector`：查询向量<br>`top_k`：返回数量<br>`filter`：过滤条件 | `results`：向量搜索结果列表 | 向量相似度搜索 |
| `InsertVector` | `collection_name`：集合名称<br>`vectors`：向量数据列表 | `success`：操作是否成功<br>`count`：插入数量<br>`error_msg`：错误信息 | 插入向量数据 |
