# Stage: voice_process

Run the voice post-production chain: normalize format, segment speech, select reference track, normalize loudness, standardize EQ, apply light compression, optional room tone normalization, crossfade merge, and export `final_voice.wav`.
